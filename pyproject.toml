[project]
name = "x32-cue-utilities"
version = "0.1.0"
description = "Generate X32 snippets and QLab network cues from a cue tracking spreadsheet."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "pandas==3.0.0",
    "openpyxl==3.1.5",
    "python-dotenv==1.2.2",
]
